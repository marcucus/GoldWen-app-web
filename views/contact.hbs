<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    
    <!-- SEO Meta Tags -->
    <title>{{title}}</title>
    <meta name="description" content="{{description}}">
    <meta name="author" content="{{app.name}} Team">
    <meta name="robots" content="index, follow">
    
    <!-- Favicon -->
    <link rel="icon" type="image/x-icon" href="/images/favicon.ico">
    
    <!-- Preconnect to external domains for performance -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    
    <!-- CSS -->
    <link href="/css/styles.css" rel="stylesheet">
    
    <!-- Theme Detection Script - Must be in head to prevent FOUC -->
    <script>
        // Theme detection and application - prevents FOUC
        (function() {
            const getTheme = () => {
                const saved = localStorage.getItem('goldwen-theme');
                if (saved && saved !== 'system') return saved;
                return window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light';
            };
            
            const applyTheme = (theme) => {
                document.documentElement.classList.toggle('dark', theme === 'dark');
            };
            
            // Apply theme immediately
            const currentTheme = getTheme();
            applyTheme(currentTheme);
            
            // Set initial theme preference
            window.__goldwen_theme = localStorage.getItem('goldwen-theme') || 'system';
        })();
    </script>
</head>
<body class="font-sans text-gray-text bg-cream-light dark:text-dark-text dark:bg-dark-primary transition-colors duration-300">
    
    <!-- Navigation -->
    <nav class="fixed top-0 w-full z-50 glass-effect bg-white/95 dark:bg-dark-secondary/95 border-b border-cream-dark dark:border-dark-tertiary transition-all duration-300">
        <div class="container-responsive">
            <div class="flex items-center justify-between h-16">
                <!-- Logo -->
                <div class="flex items-center space-x-3">
                    <a href="/" class="flex items-center space-x-3 group">
                        <div class="w-10 h-10 rounded-full flex items-center justify-center shadow-lg transition-transform duration-300 group-hover:scale-105">
                            <img src="/images/logo_light.png" alt="GoldWen Logo" class="w-10 h-10 object-contain dark:hidden">
                            <img src="/images/logo_dark.png" alt="GoldWen Logo" class="w-10 h-10 object-contain hidden dark:block">
                        </div>
                        <span class="font-serif font-bold text-xl text-gray-text dark:text-dark-text hover:text-gold-primary transition-colors duration-300">{{app.name}}</span>
                    </a>
                </div>
                
                <!-- Desktop menu -->
                <div class="hidden md:flex items-center space-x-8">
                    <a href="/" class="text-gray-text hover:text-gold-primary transition-all duration-300 font-medium relative group">
                        Accueil
                        <span class="absolute -bottom-1 left-0 w-0 h-0.5 bg-gold-primary transition-all duration-300 group-hover:w-full"></span>
                    </a>
                    <a href="/support" class="text-gray-text hover:text-gold-primary transition-all duration-300 font-medium relative group">
                        Support
                        <span class="absolute -bottom-1 left-0 w-0 h-0.5 bg-gold-primary transition-all duration-300 group-hover:w-full"></span>
                    </a>
                    <a href="/contact" class="text-gold-primary font-medium relative">
                        Contact
                        <span class="absolute -bottom-1 left-0 w-full h-0.5 bg-gold-primary"></span>
                    </a>
                    <a href="/#download" class="btn-primary text-sm px-6 py-2.5 shadow-md hover:shadow-lg transform hover:-translate-y-0.5 transition-all duration-300">
                        Télécharger
                    </a>
                </div>
                
                <!-- Mobile menu button -->
                <button id="mobile-menu-button" class="md:hidden text-gray-text hover:text-gold-primary transition-colors duration-300 p-2 rounded-lg hover:bg-cream-dark focus:outline-none focus:ring-2 focus:ring-gold-primary focus:ring-opacity-50">
                    <svg id="menu-icon" class="w-6 h-6 transition-transform duration-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
                    </svg>
                    <svg id="close-icon" class="w-6 h-6 transition-transform duration-300 hidden" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
                    </svg>
                </button>
            </div>
            
            <!-- Mobile menu -->
            <div id="mobile-menu" class="md:hidden opacity-0 max-h-0 overflow-hidden transition-all duration-300 ease-in-out">
                <div class="px-2 pt-2 pb-3 space-y-1 bg-white/95 backdrop-blur-sm rounded-lg mt-2 border border-cream-dark shadow-lg">
                    <a href="/" class="block px-4 py-3 text-gray-text hover:text-gold-primary hover:bg-cream-light transition-all duration-300 rounded-md font-medium">
                        Accueil
                    </a>
                    <a href="/support" class="block px-4 py-3 text-gray-text hover:text-gold-primary hover:bg-cream-light transition-all duration-300 rounded-md font-medium">
                        Support
                    </a>
                    <a href="/contact" class="block px-4 py-3 text-gold-primary bg-cream-light rounded-md font-medium">
                        Contact
                    </a>
                    <a href="/#download" class="block mx-4 my-2 text-center btn-primary text-sm py-3 rounded-md shadow-md hover:shadow-lg transform hover:-translate-y-0.5 transition-all duration-300">
                        Télécharger
                    </a>
                </div>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <main class="pt-24 pb-16">
        <!-- Header Section -->
        <section class="bg-gradient-to-br from-cream-light via-cream-default to-beige-light py-16">
            <div class="container-responsive">
                <div class="text-center max-w-4xl mx-auto space-calm-lg fade-in">
                    <h1 class="font-serif font-bold text-4xl md:text-5xl text-gray-text mb-6 gold-accent">
                        Contactez-nous
                    </h1>
                    <p class="text-xl text-gray-medium leading-relaxed mb-8">
                        Notre équipe est là pour vous accompagner. N'hésitez pas à nous faire part de vos questions, suggestions ou commentaires.
                    </p>
                </div>
            </div>
        </section>

        <!-- Contact Form and Info -->
        <section class="py-16 bg-white">
            <div class="container-responsive">
                <div class="grid lg:grid-cols-3 gap-12">
                    
                    <!-- Contact Form -->
                    <div class="lg:col-span-2">
                        <div class="bg-gradient-to-r from-cream-light to-beige-light rounded-xl p-8 shadow-sm">
                            <h2 class="font-serif font-bold text-2xl text-gray-text mb-6 gold-accent">
                                Envoyez-nous un message
                            </h2>
                            
                            <form id="contact-form" class="space-y-6">
                                <!-- Subject Selection -->
                                <div>
                                    <label for="subject" class="block text-sm font-medium text-gray-text mb-2">
                                        Sujet de votre demande *
                                    </label>
                                    <select id="subject" name="subject" required class="w-full px-4 py-3 border border-cream-dark rounded-lg focus:ring-2 focus:ring-gold-primary focus:border-transparent transition-all duration-300 bg-white">
                                        <option value="">Sélectionnez un sujet</option>
                                        <option value="support">Support technique</option>
                                        <option value="account">Problème de compte</option>
                                        <option value="billing">Facturation et abonnement</option>
                                        <option value="feature">Suggestion d'amélioration</option>
                                        <option value="bug">Signaler un bug</option>
                                        <option value="privacy">Confidentialité et données</option>
                                        <option value="media">Demande média</option>
                                        <option value="partnership">Partenariat</option>
                                        <option value="other">Autre</option>
                                    </select>
                                </div>

                                <!-- Name -->
                                <div class="grid md:grid-cols-2 gap-4">
                                    <div>
                                        <label for="firstName" class="block text-sm font-medium text-gray-text mb-2">
                                            Prénom *
                                        </label>
                                        <input type="text" id="firstName" name="firstName" required 
                                               class="w-full px-4 py-3 border border-cream-dark rounded-lg focus:ring-2 focus:ring-gold-primary focus:border-transparent transition-all duration-300"
                                               placeholder="Votre prénom">
                                    </div>
                                    <div>
                                        <label for="lastName" class="block text-sm font-medium text-gray-text mb-2">
                                            Nom *
                                        </label>
                                        <input type="text" id="lastName" name="lastName" required 
                                               class="w-full px-4 py-3 border border-cream-dark rounded-lg focus:ring-2 focus:ring-gold-primary focus:border-transparent transition-all duration-300"
                                               placeholder="Votre nom">
                                    </div>
                                </div>

                                <!-- Email -->
                                <div>
                                    <label for="email" class="block text-sm font-medium text-gray-text mb-2">
                                        Adresse email *
                                    </label>
                                    <input type="email" id="email" name="email" required 
                                           class="w-full px-4 py-3 border border-cream-dark rounded-lg focus:ring-2 focus:ring-gold-primary focus:border-transparent transition-all duration-300"
                                           placeholder="votre.email@exemple.com">
                                </div>

                                <!-- User Type -->
                                <div>
                                    <label for="userType" class="block text-sm font-medium text-gray-text mb-2">
                                        Vous êtes
                                    </label>
                                    <select id="userType" name="userType" class="w-full px-4 py-3 border border-cream-dark rounded-lg focus:ring-2 focus:ring-gold-primary focus:border-transparent transition-all duration-300 bg-white">
                                        <option value="prospect">Intéressé(e) par GoldWen</option>
                                        <option value="user">Utilisateur de GoldWen</option>
                                        <option value="plus">Abonné GoldWen Plus</option>
                                        <option value="former">Ancien utilisateur</option>
                                        <option value="media">Journaliste / Média</option>
                                        <option value="partner">Partenaire potentiel</option>
                                        <option value="other">Autre</option>
                                    </select>
                                </div>

                                <!-- Message -->
                                <div>
                                    <label for="message" class="block text-sm font-medium text-gray-text mb-2">
                                        Votre message *
                                    </label>
                                    <textarea id="message" name="message" rows="6" required 
                                              class="w-full px-4 py-3 border border-cream-dark rounded-lg focus:ring-2 focus:ring-gold-primary focus:border-transparent transition-all duration-300 resize-none"
                                              placeholder="Décrivez votre demande en détail..."></textarea>
                                    <p class="text-xs text-gray-medium mt-2">Minimum 10 caractères</p>
                                </div>

                                <!-- Consent -->
                                <div class="flex items-start space-x-3">
                                    <input type="checkbox" id="consent" name="consent" required 
                                           class="mt-1 w-4 h-4 text-gold-primary border-cream-dark rounded focus:ring-gold-primary">
                                    <label for="consent" class="text-sm text-gray-medium">
                                        J'accepte que mes données soient utilisées pour traiter ma demande conformément à la 
                                        <a href="/confidentialite" class="text-gold-primary hover:underline">politique de confidentialité</a>. *
                                    </label>
                                </div>

                                <!-- Submit Button -->
                                <div class="pt-4">
                                    <button type="submit" 
                                            class="w-full btn-primary py-4 text-lg font-medium shadow-lg hover:shadow-xl transform hover:-translate-y-1 transition-all duration-300 disabled:opacity-50 disabled:cursor-not-allowed disabled:transform-none">
                                        <span id="submit-text">Envoyer le message</span>
                                        <span id="submit-loading" class="hidden">Envoi en cours...</span>
                                    </button>
                                </div>
                            </form>

                            <!-- Success Message -->
                            <div id="success-message" class="hidden mt-6 p-4 bg-green-50 border border-green-200 rounded-lg">
                                <div class="flex items-center space-x-3">
                                    <svg class="w-5 h-5 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                                    </svg>
                                    <p class="text-green-800">
                                        <strong>Message envoyé avec succès !</strong><br>
                                        Nous vous répondrons dans les plus brefs délais.
                                    </p>
                                </div>
                            </div>

                            <!-- Error Message -->
                            <div id="error-message" class="hidden mt-6 p-4 bg-red-50 border border-red-200 rounded-lg">
                                <div class="flex items-center space-x-3">
                                    <svg class="w-5 h-5 text-red-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-2.5L13.732 4c-.77-.833-1.964-.833-2.732 0L4.082 15.5c-.77.833.192 2.5 1.732 2.5z"></path>
                                    </svg>
                                    <p class="text-red-800">
                                        <strong>Erreur lors de l'envoi.</strong><br>
                                        Veuillez réessayer ou nous contacter directement par email.
                                    </p>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Contact Information -->
                    <div class="space-y-8">
                        <!-- Contact Methods -->
                        <div class="bg-white rounded-xl p-6 shadow-sm border border-cream-dark">
                            <h3 class="font-serif font-semibold text-xl text-gray-text mb-6 gold-accent">
                                Autres moyens de contact
                            </h3>
                            
                            <div class="space-y-6">
                                <div class="flex items-start space-x-4">
                                    <div class="w-10 h-10 bg-gold-primary rounded-full flex items-center justify-center flex-shrink-0">
                                        <svg class="w-5 h-5 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 4.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"></path>
                                        </svg>
                                    </div>
                                    <div>
                                        <h4 class="font-semibold text-gray-text">Email général</h4>
                                        <p class="text-gray-medium">contact@goldwen.app</p>
                                        <p class="text-xs text-gray-medium mt-1">Réponse sous 24h</p>
                                    </div>
                                </div>

                                <div class="flex items-start space-x-4">
                                    <div class="w-10 h-10 bg-gold-primary rounded-full flex items-center justify-center flex-shrink-0">
                                        <svg class="w-5 h-5 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M18.364 5.636l-3.536 3.536m0 5.656l3.536 3.536M9.172 9.172L5.636 5.636m3.536 9.192L5.636 18.364M21 12a9 9 0 11-18 0 9 9 0 0118 0zm-5 0a4 4 0 11-8 0 4 4 0 018 0z"></path>
                                        </svg>
                                    </div>
                                    <div>
                                        <h4 class="font-semibold text-gray-text">Support technique</h4>
                                        <p class="text-gray-medium">support@goldwen.app</p>
                                        <p class="text-xs text-gray-medium mt-1">Réponse sous 12h</p>
                                    </div>
                                </div>

                                <div class="flex items-start space-x-4">
                                    <div class="w-10 h-10 bg-gold-primary rounded-full flex items-center justify-center flex-shrink-0">
                                        <svg class="w-5 h-5 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 20H5a2 2 0 01-2-2V6a2 2 0 012-2h10a2 2 0 012 2v1m2 13a2 2 0 01-2-2V7m2 13a2 2 0 002-2V9.5a2.5 2.5 0 00-2.5-2.5H15"></path>
                                        </svg>
                                    </div>
                                    <div>
                                        <h4 class="font-semibold text-gray-text">Presse & Média</h4>
                                        <p class="text-gray-medium">media@goldwen.app</p>
                                        <p class="text-xs text-gray-medium mt-1">Kit presse disponible</p>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <!-- Response Time -->
                        <div class="bg-gradient-to-r from-gold-primary/10 to-gold-light/10 rounded-xl p-6 border border-gold-primary/20">
                            <h3 class="font-semibold text-gray-text mb-3 flex items-center">
                                <svg class="w-5 h-5 mr-2 text-gold-primary" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                                </svg>
                                Temps de réponse
                            </h3>
                            <ul class="text-sm text-gray-medium space-y-2">
                                <li>• <strong>Urgences :</strong> Sous 2h en jours ouvrés</li>
                                <li>• <strong>Support :</strong> Sous 12h</li>
                                <li>• <strong>Demandes générales :</strong> Sous 24h</li>
                                <li>• <strong>Partenariats :</strong> Sous 48h</li>
                            </ul>
                        </div>

                        <!-- FAQ Link -->
                        <div class="bg-beige-light rounded-xl p-6">
                            <h3 class="font-semibold text-gray-text mb-3">Besoin d'aide rapide ?</h3>
                            <p class="text-gray-medium text-sm mb-4">
                                Consultez notre centre d'aide pour trouver des réponses immédiates aux questions les plus fréquentes.
                            </p>
                            <a href="/support" class="btn-secondary text-sm inline-flex items-center">
                                <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8.228 9c.549-1.165 2.03-2 3.772-2 2.21 0 4 1.343 4 3 0 1.4-1.278 2.575-3.006 2.907-.542.104-.994.54-.994 1.093m0 3h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                                </svg>
                                Centre d'aide
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </section>
    </main>

    <!-- Footer -->
    <footer class="bg-gray-text text-white py-12">
        <div class="container-responsive">
            <div class="grid md:grid-cols-4 gap-8">
                <div class="col-span-2">
                    <div class="flex items-center space-x-2 mb-4">
                        <div class="w-8 h-8 rounded-full flex items-center justify-center">
                            <img src="/images/logo_light.png" alt="GoldWen Logo" class="w-8 h-8 object-contain">
                        </div>
                        <span class="font-serif font-bold text-xl">{{app.name}}</span>
                    </div>
                    <p class="text-gray-300 mb-4">{{app.slogan}}</p>
                    <p class="text-gray-400 text-sm">
                        L'application de rencontre qui privilégie la qualité des connexions 
                        pour des relations authentiques et durables.
                    </p>
                </div>
                <div>
                    <h4 class="font-semibold mb-4">Application</h4>
                    <ul class="space-y-2 text-gray-300">
                        <li><a href="/" class="hover:text-gold-primary transition-colors">Accueil</a></li>
                        <li><a href="/#features" class="hover:text-gold-primary transition-colors">Fonctionnalités</a></li>
                        <li><a href="/#about" class="hover:text-gold-primary transition-colors">À propos</a></li>
                        <li><a href="/support" class="hover:text-gold-primary transition-colors">Support</a></li>
                    </ul>
                </div>
                <div>
                    <h4 class="font-semibold mb-4">Légal</h4>
                    <ul class="space-y-2 text-gray-300">
                        <li><a href="/confidentialite" class="hover:text-gold-primary transition-colors">Confidentialité</a></li>
                        <li><a href="/conditions" class="hover:text-gold-primary transition-colors">Conditions d'utilisation</a></li>
                        <li><a href="/mentions-legales" class="hover:text-gold-primary transition-colors">Mentions légales</a></li>
                        <li><a href="/contact" class="text-gold-primary">Contact</a></li>
                    </ul>
                </div>
            </div>
            <div class="border-t border-gray-600 mt-8 pt-8 text-center text-gray-400 text-sm">
                <p>&copy; 2025 {{app.name}}. Tous droits réservés.</p>
            </div>
        </div>
    </footer>

    <!-- JavaScript for mobile menu and form handling -->
    <script>
        // Mobile menu toggle functionality
        const initMobileMenu = () => {
            const mobileMenuButton = document.getElementById('mobile-menu-button');
            const mobileMenu = document.getElementById('mobile-menu');
            const menuIcon = document.getElementById('menu-icon');
            const closeIcon = document.getElementById('close-icon');
            let isMenuOpen = false;

            const toggleMenu = () => {
                isMenuOpen = !isMenuOpen;
                
                if (isMenuOpen) {
                    mobileMenu.classList.remove('opacity-0', 'max-h-0');
                    mobileMenu.classList.add('opacity-100', 'max-h-96');
                    menuIcon.classList.add('hidden');
                    closeIcon.classList.remove('hidden');
                } else {
                    mobileMenu.classList.add('opacity-0', 'max-h-0');
                    mobileMenu.classList.remove('opacity-100', 'max-h-96');
                    menuIcon.classList.remove('hidden');
                    closeIcon.classList.add('hidden');
                }
            };

            mobileMenuButton?.addEventListener('click', toggleMenu);

            // Close menu when clicking outside
            document.addEventListener('click', (e) => {
                if (isMenuOpen && !mobileMenuButton?.contains(e.target) && !mobileMenu?.contains(e.target)) {
                    toggleMenu();
                }
            });
        };

        // Enhanced navbar scroll behavior
        const initNavbarScroll = () => {
            const navbar = document.querySelector('nav');

            const updateNavbar = () => {
                const scrollY = window.scrollY;
                
                if (scrollY > 50) {
                    navbar.classList.add('shadow-lg');
                    navbar.classList.remove('border-b');
                } else {
                    navbar.classList.remove('shadow-lg');
                    navbar.classList.add('border-b');
                }
            };

            window.addEventListener('scroll', updateNavbar);
        };

        // Contact form handling
        const initContactForm = () => {
            const form = document.getElementById('contact-form');
            const submitBtn = form.querySelector('button[type="submit"]');
            const submitText = document.getElementById('submit-text');
            const submitLoading = document.getElementById('submit-loading');
            const successMessage = document.getElementById('success-message');
            const errorMessage = document.getElementById('error-message');

            form.addEventListener('submit', async (e) => {
                e.preventDefault();
                
                // Hide previous messages
                successMessage.classList.add('hidden');
                errorMessage.classList.add('hidden');
                
                // Show loading state
                submitBtn.disabled = true;
                submitText.classList.add('hidden');
                submitLoading.classList.remove('hidden');
                
                // Get form data
                const formData = new FormData(form);
                const data = Object.fromEntries(formData);
                
                // Basic validation
                if (data.message.length < 10) {
                    errorMessage.classList.remove('hidden');
                    errorMessage.querySelector('p').innerHTML = '<strong>Message trop court.</strong><br>Veuillez saisir au moins 10 caractères.';
                    resetSubmitButton();
                    return;
                }
                
                try {
                    // Simulate form submission (in a real app, this would be an API call)
                    await new Promise(resolve => setTimeout(resolve, 2000));
                    
                    // Success
                    successMessage.classList.remove('hidden');
                    form.reset();
                    
                    // Log to console for demonstration
                    console.log('Contact form submitted:', data);
                    
                } catch (error) {
                    // Error
                    errorMessage.classList.remove('hidden');
                }
                
                resetSubmitButton();
            });
            
            function resetSubmitButton() {
                submitBtn.disabled = false;
                submitText.classList.remove('hidden');
                submitLoading.classList.add('hidden');
            }
        };

        // Form validation enhancements
        const initFormValidation = () => {
            const requiredFields = document.querySelectorAll('input[required], select[required], textarea[required]');
            
            requiredFields.forEach(field => {
                field.addEventListener('blur', () => {
                    if (field.value.trim() === '') {
                        field.classList.add('border-red-300');
                        field.classList.remove('border-cream-dark');
                    } else {
                        field.classList.remove('border-red-300');
                        field.classList.add('border-cream-dark');
                    }
                });
                
                field.addEventListener('input', () => {
                    if (field.value.trim() !== '') {
                        field.classList.remove('border-red-300');
                        field.classList.add('border-cream-dark');
                    }
                });
            });
        };

        // Initialize all functionality when DOM is loaded
        document.addEventListener('DOMContentLoaded', () => {
            initMobileMenu();
            initNavbarScroll();
            initContactForm();
            initFormValidation();
        });
    </script>
</body>
</html>